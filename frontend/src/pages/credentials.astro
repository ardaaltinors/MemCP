---
import Layout from "layouts/Layout.astro";
import { DashboardHeader } from "@libs/ui/components/DashboardHeader";
import { CredentialsContainer } from "@libs/ui/components/CredentialsContainer";
---

<Layout
    title="API Keys & Connection Management - MemCP"
    description="Manage your API keys and MCP client connections for the MemCP platform."
    hideHeader={true}
>
    <main class="min-h-screen bg-[#0D0D1A] overflow-x-hidden">
        <DashboardHeader currentPage="credentials" client:only="react" />
        
        {/* Credentials Container */}
        <CredentialsContainer client:only="react" />
    </main>
</Layout>

<style>
    /* Enhanced dark mode styles for Apple-like aesthetic */
    main {
        font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Inter', sans-serif;
    }

    /* Enhanced gradient background */
    main::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-image: 
            radial-gradient(circle 600px at 20% 80%, rgba(99, 102, 241, 0.15), transparent 70%),
            radial-gradient(circle 800px at 80% 20%, rgba(139, 92, 246, 0.1), transparent 70%),
            radial-gradient(circle 1000px at 50% 50%, rgba(59, 130, 246, 0.05), transparent 70%);
        pointer-events: none;
        z-index: 0;
    }

    /* Ensure content is above gradient */
    main > * {
        position: relative;
        z-index: 1;
    }
</style>